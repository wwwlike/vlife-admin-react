/*vlife*/
import { Input } from "@douyinfe/semi-ui";
import { InputProps } from "@douyinfe/semi-ui/lib/es/input";
import React, { ReactNode } from "react";

export default (prop: InputProps) => {
  return <Input {...prop} />;
};
